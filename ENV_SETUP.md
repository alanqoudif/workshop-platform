# 🔐 دليل إعداد المتغيرات البيئية

## ✅ تم الحصول على بيانات Supabase تلقائياً!

تم استخدام **Supabase MCP** للحصول على المتغيرات البيئية الخاصة بمشروعك.

---

## 📋 الخطوات السريعة

### 1. إنشاء ملف `.env.local`

```bash
cp .env.example .env.local
```

**ملاحظة:** الملف `.env.example` يحتوي بالفعل على بيانات Supabase الصحيحة! ✅

---

## 🔑 المتغيرات البيئية

### ✅ Supabase (جاهز!)

```env
NEXT_PUBLIC_SUPABASE_URL=https://kgiinginettubtntecdn.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

**الحالة:** ✅ جاهز للاستخدام مباشرة!

---

### ⚠️ WhatsApp API (يحتاج إعداد)

#### الخطوات:

1. **التسجيل في Automapi**
   - اذهب إلى: https://automapi.com
   - أنشئ حساب جديد
   - قم بربط رقم WhatsApp الخاص بك

2. **الحصول على المفاتيح**
   - من لوحة التحكم، احصل على:
     - `Instance ID`
     - `Access Token`

3. **تحديث `.env.local`**
   ```env
   WHATSAPP_INSTANCE_ID=68D1B85608A7E  # مثال
   WHATSAPP_ACCESS_TOKEN=660f1622e1665  # مثال
   ```

#### تنسيق الرقم الصحيح:
```
✅ صحيح: 966501234567
❌ خطأ: +966501234567
❌ خطأ: 00966501234567
```

---

### ⚠️ Stripe (اختياري - للاشتراكات)

#### الخطوات:

1. **التسجيل في Stripe**
   - اذهب إلى: https://dashboard.stripe.com
   - أنشئ حساب جديد

2. **الحصول على مفاتيح الاختبار**
   - من Dashboard → Developers → API keys
   - احصل على:
     - `Secret key` (sk_test_...)
     - `Publishable key` (pk_test_...)

3. **إعداد Webhook**
   - من Dashboard → Developers → Webhooks
   - أضف endpoint: `http://localhost:3000/api/webhooks/stripe`
   - احصل على `Webhook secret` (whsec_...)

4. **تحديث `.env.local`**
   ```env
   STRIPE_SECRET_KEY=sk_test_...
   STRIPE_WEBHOOK_SECRET=whsec_...
   NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...
   ```

**ملاحظة:** Stripe اختياري - المنصة تعمل بدونه!

---

## 🚀 التشغيل

بعد إعداد `.env.local`:

```bash
npm run dev
```

افتح المتصفح على: http://localhost:3000

---

## ✅ قائمة التحقق

- [x] ✅ Supabase URL
- [x] ✅ Supabase Anon Key
- [ ] ⚠️ WhatsApp Instance ID
- [ ] ⚠️ WhatsApp Access Token
- [ ] ⏳ Stripe Keys (اختياري)

---

## 🔍 التحقق من الإعداد

### اختبار Supabase
```bash
# سيعمل مباشرة - Supabase جاهز!
npm run dev
# افتح http://localhost:3000/login
```

### اختبار WhatsApp
1. سجل في ورشة برقم هاتفك
2. اقبل التسجيل من لوحة التحكم
3. تحقق من وصول رسالة WhatsApp

### اختبار Stripe
1. اذهب إلى صفحة الاشتراكات (عند إضافتها)
2. استخدم بطاقة اختبار: `4242 4242 4242 4242`

---

## 🆘 المشاكل الشائعة

### المشكلة: Supabase لا يعمل
**الحل:** تأكد من نسخ `.env.example` إلى `.env.local`

### المشكلة: WhatsApp لا يرسل
**الحل:** 
1. تحقق من صحة `instance_id` و `access_token`
2. تحقق من تنسيق الرقم (966...)
3. تحقق من رصيد حساب Automapi

### المشكلة: Stripe لا يعمل
**الحل:**
1. تأكد من استخدام مفاتيح الاختبار (test keys)
2. تأكد من إعداد Webhook بشكل صحيح

---

## 📝 ملاحظات أمان

- ✅ `.env.local` مستثنى من Git تلقائياً
- ✅ لا تشارك مفاتيحك مع أحد
- ✅ استخدم مفاتيح الاختبار للتطوير
- ✅ استخدم مفاتيح الإنتاج فقط على السيرفر

---

## 🎉 الخلاصة

**Supabase جاهز للاستخدام مباشرة!** ✅

فقط قم بـ:
1. نسخ `.env.example` إلى `.env.local`
2. تحديث بيانات WhatsApp
3. تشغيل `npm run dev`

**المنصة ستعمل مباشرة!** 🚀

---

**تم إنشاء هذا الملف تلقائياً باستخدام Supabase MCP** ✨

